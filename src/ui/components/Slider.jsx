import { Slider as RadixSlider } from '@radix-ui/themes'

/**
 * Slider Component
 * 
 * Wraps Radix UI Slider for easy customization.
 * Edit this file to customize slider styling.
 */
export const Slider = ({ ...props }) => {
  return <RadixSlider {...props} />
}

export default Slider

